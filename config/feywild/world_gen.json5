{
  "biomes": {
  
    "autumn": {
    
      // Size of a autumn biome
      // Range: 0.0 - 1.0
      "size": 0.003,
      
      // Weight for the autumn biome to be generated
      // Minimum: 0
      "weight": 2
    },
    
    "spring": {
    
      // Size of a spring biome
      // Range: 0.0 - 1.0
      "size": 0.003,
      
      // Weight for the spring biome to be generated
      // Minimum: 0
      "weight": 2
    },
    
    "summer": {
    
      // Size of a summer biome
      // Range: 0.0 - 1.0
      "size": 0.003,
      
      // Weight for the summer biome to be generated
      // Minimum: 0
      "weight": 2
    },
    
    "winter": {
    
      // Size of a winter biome
      // Range: 0.0 - 1.0
      "size": 0.003,
      
      // Weight for the winter biome to be generated
      // Minimum: 0
      "weight": 2
    }
  },
  
  "ores": {
  
    "fey_gem": {
    
      // Maximum height for fey gems veins
      // Range: 0 - 255
      "max_height": 25,
      
      // Minimum height for fey gems veins
      // Range: 0 - 255
      "min_height": 11,
      
      // Size for fey gems veins
      // Minimum: 0
      "size": 2,
      
      // Weight for fey gems to spawn
      // Minimum: 0
      "weight": 10
    }
  },
  
  "structures": {
  
    "autumn_world_tree": {
    
      // The average distance between two autumn world trees. Must be higher than minimum.
      // Minimum: 1
      "average_distance": 1000,
      
      // The minimum distance between two autumn world trees
      // Minimum: 1
      "minimum_distance": 300
    },
    
    "bee_keep": {
    
      // The average distance between two bee keeps. Must be higher than minimum.
      // Minimum: 1
      "average_distance": 30,
      
      // The amount of time in seconds that the magical honey needs to respawn
      // Minimum: 1
      "honey_timer": 1200,
      
      // The minimum distance between two  bee keeps
      // Minimum: 1
      "minimum_distance": 20
    },
    
    "blacksmith": {
    
      // The average distance between two blacksmiths. Must be higher than minimum.
      // Minimum: 1
      "average_distance": 35,
      
      // The minimum distance between two blacksmiths
      // Minimum: 1
      "minimum_distance": 25
    },
    
    "library": {
    
      // The average distance between two libraries. Must be higher than minimum.
      // Minimum: 1
      "average_distance": 35,
      
      // The minimum distance between two libraries
      // Minimum: 1
      "minimum_distance": 25
    },
    
    "spring_world_tree": {
    
      // The average distance between two spring world trees. Must be higher than minimum.
      // Minimum: 1
      "average_distance": 1000,
      
      // The minimum distance between two spring world trees
      // Minimum: 1
      "minimum_distance": 300
    },
    
    "summer_world_tree": {
    
      // The average distance between two summer world trees. Must be higher than minimum.
      // Minimum: 1
      "average_distance": 1000,
      
      // The minimum distance between two summer world trees
      // Minimum: 1
      "minimum_distance": 300
    },
    
    "winter_world_tree": {
    
      // The average distance between two winter world trees. Must be higher than minimum.
      // Minimum: 1
      "average_distance": 1000,
      
      // The minimum distance between two winter world trees
      // Minimum: 1
      "minimum_distance": 300
    }
  },
  
  "tree_patches": {
  
    // Whether autumn tree patches should be enabled
    "autumn": false,
    
    // Tree patch spawn chance.
    // Range: 0.0 - 1.0
    "chance": 0.01,
    
    // Tree patch spawn size.
    // Minimum: 1
    "size": 3,
    
    // Whether spring tree patches should be enabled
    "spring": false,
    
    // Whether summer tree patches should be enabled
    "summer": false,
    
    // Whether winter tree patches should be enabled
    "winter": false
  }
}

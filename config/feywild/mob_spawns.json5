{
  "autumn_pixie": {
  
    // Biome types where the entity should spawn
    "biomes": [
      "SWAMP",
      "MUSHROOM",
      "SPOOKY",
      "FOREST",
      "MAGICAL"
    ],
    
    // Maximum entities to spawn in a group group
    // Minimum: 1
    "max": 1,
    
    // Minimum entities to spawn in a group group
    // Minimum: 1
    "min": 1,
    
    // Autumn pixie spawn chance
    // Minimum: 0
    "weight": 20
  },
  
  "dimensions": {
  
    // What dimension biomes should be whitelisted for Feywild mobs
    "white_list_biomes": [
      "twilightforest:forest",
      "twilightforest:dense_forest",
      "twilightforest:dense_mushroom_forest",
      "twilightforest:enchanted_forest"
    ]
  },
  
  "dwarf_blacksmith": {
  
    // Maximum entities to spawn in a group group
    // Minimum: 1
    "max": 1,
    
    // Minimum entities to spawn in a group group
    // Minimum: 1
    "min": 1,
    
    // Dwarf blacksmith spawn chance
    // Minimum: 0
    "weight": 20
  },
  
  "shroomling": {
  
    // Biome types where the entity should spawn
    "biomes": [ "MUSHROOM", "MAGICAL" ],
    
    // Maximum entities to spawn in a group group
    // Minimum: 1
    "max": 1,
    
    // Minimum entities to spawn in a group group
    // Minimum: 1
    "min": 1,
    
    // Shroomling spawn chance
    // Minimum: 0
    "weight": 15
  },
  
  "spring_pixie": {
  
    // Biome types where the entity should spawn
    "biomes": [ "RIVER", "FOREST", "PLAINS", "MAGICAL" ],
    
    // Maximum entities to spawn in a group group
    // Minimum: 1
    "max": 1,
    
    // Minimum entities to spawn in a group group
    // Minimum: 1
    "min": 1,
    
    // Spring pixie spawn chance
    // Minimum: 0
    "weight": 20
  },
  
  "summer_bee_knight": {
  
    // What should be the aggrevation range of the Bee Knights, when someone tries to steal their honey!
    // Minimum: 3
    "aggrevation_range": 20,
    
    // Maximum entities to spawn in a group group
    // Minimum: 1
    "max": 2,
    
    // Minimum entities to spawn in a group group
    // Minimum: 1
    "min": 1,
    
    // How much reputation the player needs to have for the guards to not attack him.
    // Minimum: 0
    "required_reputation": 35,
    
    // Bee knight spawn chance
    // Minimum: 0
    "weight": 25
  },
  
  "summer_pixie": {
  
    // Biome types where the entity should spawn
    "biomes": [ "LUSH", "HOT", "MAGICAL" ],
    
    // Maximum entities to spawn in a group group
    // Minimum: 1
    "max": 1,
    
    // Minimum entities to spawn in a group group
    // Minimum: 1
    "min": 1,
    
    // Summer pixie spawn chance
    // Minimum: 0
    "weight": 20
  },
  
  "winter_pixie": {
  
    // Biome types where the entity should spawn
    "biomes": [ "DEAD", "SNOWY", "COLD", "MAGICAL" ],
    
    // Maximum entities to spawn in a group group
    // Minimum: 1
    "max": 1,
    
    // Minimum entities to spawn in a group group
    // Minimum: 1
    "min": 1,
    
    // Winter pixie spawn chance
    // Minimum: 0
    "weight": 20
  }
}
